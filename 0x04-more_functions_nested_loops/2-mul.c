#include <stdio.h>
#include "main.h"

/**
 * mul - checks if a letter is an uppercase
 * @a: letter to be checked
 * @b: letter to check
 * Return: 1 for uppercase and 0 for anything else
 */

int mul(int a, int b)
{
	int c;

	c = a * b;
	return (c);
}
